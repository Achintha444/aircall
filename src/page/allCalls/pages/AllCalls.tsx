import AllCallsLayout from "../layout/AllCallsLayout";

/**
 * AllCalls component to display all non-archived calls.
 * 
 * @returns JSX Element representing the layout for all non-archived calls.
 */
function AllCalls() {
    return (
        <AllCallsLayout />
    );
}

export default AllCalls;
